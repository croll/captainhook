(function(){
var CaptainHook = new new Class({

	Implements: [Events, Options],
	
	initialize: function() {
	}
});
}).call(this);

(function(){
	var CaptainHook.Form.Autocomplete = new Class({

	Implements: [Events, Options],

	initialize: function(options) {
		this.setOptions(options);
	}

	});
});
